(()=>{'use strict';const card=document.querySelector('.card.word');if(!card)return;const toggle=document.createElement('button'),hint=document.createElement('p');toggle.id='revealWord';toggle.className='recall-toggle';toggle.textContent='显示释义，再判断是否掌握';hint.className='word-answer-hint';hint.textContent='先在心里说出词义，再点按钮核对。';const line=document.querySelector('.word-title-controls')||document.getElementById('phonetic');line.after(toggle,hint);function hide(){card.classList.add('recall-hidden');toggle.hidden=false;toggle.textContent='显示释义，再判断是否掌握'}function reveal(){card.classList.remove('recall-hidden');toggle.hidden=true;document.getElementById('meaning').scrollIntoView({block:'nearest',behavior:'smooth'})}toggle.onclick=reveal;const previous=window.word;window.word=function(){previous();hide()};hide();
const known=new Set(words.map(x=>String(x.a||'').toLowerCase()));for(const x of Object.keys(window.FULL_EXAM_DICTIONARY||{}))known.add(x);function splitOne(raw){const lower=raw.toLowerCase();if(known.has(lower)||lower.length<7||/[^a-z]/.test(lower))return [raw];const n=lower.length,dp=Array(n+1).fill(null);dp[0]=[];for(let i=0;i<n;i++)if(dp[i])for(let j=i+1;j<=n;j++){const part=lower.slice(i,j);if((part.length>=2||part==='a'||part==='i')&&known.has(part)){const next=[...dp[i],raw.slice(i,j)];if(!dp[j]||next.length<dp[j].length)dp[j]=next}}return dp[n]&&dp[n].length>1?dp[n]:[raw]}
function cleanContexts(){document.querySelectorAll('#exam-source-card li span').forEach(span=>{if(span.dataset.cleaned)return;const pieces=(span.textContent.match(/[A-Za-z]+(?:[-'][A-Za-z]+)*|[^A-Za-z]+/g)||[]).flatMap(x=>/^[A-Za-z]+$/.test(x)?splitOne(x):[x]);const clean=pieces.join(' ').replace(/\s+([,.;:!?])/g,'$1').replace(/\s+/g,' ').trim();span.innerHTML=clean.replace(/([A-Za-z]+(?:[-'][A-Za-z]+)*)/g,'<span class="tap-word" data-word="$1">$1</span>');span.dataset.cleaned='1';const current=words[st.i],gloss=document.createElement('small');gloss.className='context-gloss';gloss.innerHTML='<b>中文理解：</b>本卡核心词 <b>'+current.a+'</b> 在这里按“'+current.c+'”理解。上方是题目/选项附近的短片段，可逐词点击查义；不把残缺片段硬拼成所谓标准译文。';span.after(gloss)})}
const currentWord=window.word;window.word=function(){currentWord();setTimeout(cleanContexts,0)};setTimeout(cleanContexts,0)})();
