(function(){'use strict';const D=window.ENGLISH_EXPANSION_DATA,$=s=>document.querySelector(s);let tab='roots',index=0,q='';const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function list(){const a=tab==='roots'?D.roots:tab==='topics'?D.topics:D.groups;return a.filter(x=>!q||(JSON.stringify(x).toLowerCase().includes(q.toLowerCase())))}function row(w){return `<div class="block"><b class="word">${esc(w.word)}</b> <span class="muted">/${esc(w.phonetic||'')}/</span><p>${esc(w.meaning)}</p><p class="muted">${w.years.length?`英语一真题覆盖 ${w.years.length} 年，共 ${w.count} 次：${w.years.join('、')}`:'补充词'}</p>${w.context?`<p>${esc(w.context)}</p>`:''}${w.translation?`<p class="muted">${esc(w.translation)}</p>`:''}</div>`}function render(){const a=list();if(!a.length){$('#content').innerHTML='<article class="card">没有匹配内容。</article>';return}index=Math.min(index,a.length-1);const x=a[index];$('#chooser').innerHTML=a.map((y,i)=>`<option value="${i}">${esc(y.root||y.name)}</option>`).join('');$('#chooser').value=index;$('#pageInfo').textContent=`${index+1}/${a.length}`;if(tab==='roots')$('#content').innerHTML=`<article class="card"><small>英语一真题构词关联</small><h2>${esc(x.root)} = ${esc(x.meaning)}</h2><p class="muted">已按英语一真题覆盖年份和次数排序；只保留构词候选，词义以真题语境为准。</p>${x.words.map(row).join('')}</article>`;else if(tab==='topics')$('#content').innerHTML=`<article class="card"><small>英语一真题话题词</small><h2>${esc(x.name)}</h2>${x.words.map(row).join('')}</article>`;else $('#content').innerHTML=`<article class="card"><small>作文PDF完整表达整理</small><h2>${esc(x.name)}</h2>${x.items.map(p=>`<div class="pair"><strong>${esc(p[0])}</strong><span>${esc(p[1])}</span></div>`).join('')}</article>`}document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;index=0;q='';$('#search').value='';document.querySelectorAll('[data-tab]').forEach(y=>y.classList.toggle('on',y===b));render()});$('#chooser').onchange=e=>{index=+e.target.value;render()};$('#search').oninput=e=>{q=e.target.value.trim();index=0;render()};$('#prev').onclick=()=>{if(index>0){index--;render();scrollTo(0,0)}};$('#next').onclick=()=>{if(index<list().length-1){index++;render();scrollTo(0,0)}};render()})();
